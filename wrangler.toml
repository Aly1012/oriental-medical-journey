name = "oriental-medical-journey"
compatibility_date = "2026-09-16"

# Static site (东方医旅 / Oriental Medical Journey) — no build step.
# Serve everything in the repo root as a Workers static-assets project.
[assets]
directory = "./"
