name = "orientalmedicaljourney"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# Static site, no build step required.
# Deploy with:  wrangler pages deploy .
# (run from inside this medical-tourism/ directory)
